const express = require('express')
const router = express.Router()

module.exports = () => {
  //router.get('/clientes', clientsController.selectClients)
  //router.post('clientes', clientsController.createClient)
  return router
}
